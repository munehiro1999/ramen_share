<% if obj.errors.any? %>
  <div class="alert alert-danger">
    <h4><%= pluralize(obj.errors.count, "error")%>  「保存できませんでした。以下のエラーを確認してください」 </h4>
    <ul>
      <% obj.errors.full_messages.each do |message|%>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
