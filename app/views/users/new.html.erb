<h2>ユーザー登録できます</h2>

<%= form_with model: @user do |f| %>
  <div>
    <%= f.label :name, "名前" %>
    <%= f.text_field :name %>
  </div>

  <div>
    <%= f.label :email, "メールアドレス" %>
    <%= f.text_field :email %>
  </div>

  <div>
    <%= f.submit "登録完了" %>

  </div>
<% end %>
